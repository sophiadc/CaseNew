extends ../layout

block content
  .main.container
    .row
     .col-md-8.col-md-offset-2
        h1.display-4.m-b-2 All Current Users
        ul
          each user in users
           li Name: #{user.firstName} #{user.lastName}, Job: #{user.jobTitle}